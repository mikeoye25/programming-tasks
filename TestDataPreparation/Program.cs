using System;
using TestDataPreparation.Challenges;

namespace TestDataPreparation
{
    class Program
    {
        static void Main(string[] args)
        {
            Console.WriteLine("Hello World!");

            int[] items = { 2, 3, 5, 3, 7, 5 };

            var uniqueArrayItems = UniqueArrayItems.Execute(items);
            // FizzBuzz.Execute();
            // Palindrome.Execute();
            // Anagram.Execute("dear", "read");
            var mmm = "";
        }
    }
}
